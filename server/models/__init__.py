from .association import employee_meeting_association

from .employee import Employee
from .meeting import Meeting

# Model files define the structure of your database tables and the relationships between them. 
# They act as a bridge between your Python code and the database, allowing you to interact with the
# database using Python objects instead of writing raw SQL queries.

